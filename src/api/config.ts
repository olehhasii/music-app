export const API_BASE_URL = 'http://localhost:8000/api';

export const GET_TRACKS_URL = `${API_BASE_URL}/tracks`;

export const GET_GENRES_URL = `${API_BASE_URL}/genres`;

export const CREATE_TRACK_URL = `${API_BASE_URL}/tracks`;
